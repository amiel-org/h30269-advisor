# 红利低波决策台 V3.1 迁移说明

## 方案一：目标电脑有 Python

1. 把整个 h30269_advisor 目录复制到目标电脑。
2. 目标电脑安装 Python 3.11 或更高版本，安装时勾选 Add Python to PATH。
3. 双击 install_and_run.bat。
4. 首次运行会创建 .venv 并安装三个 Python 依赖，之后会自动打开 http://127.0.0.1:8765/。

## 方案二：目标电脑没有 Python

在已构建的 H30269-Advisor-portable.zip 中：

1. 解压整个目录，不要只拿出 EXE。
2. 双击 启动红利低波决策台.bat。
3. 停止服务时双击 停止红利低波决策台.bat。

免 Python 版必须保留 EXE 旁边的 static、cache、config.json 等文件，不能只复制一个 EXE。

## 目标电脑需要什么

- Windows 10/11。
- 能访问中证指数、蛋卷基金和中国债券信息网的数据接口。
- 不需要 AI、GPU、Codex、OpenAI API Key 或任何模型账号。
- 程序不会自动交易，只在本机生成规则建议。
- V3.1 的全部判断都在本地按固定规则计算，不调用任何 AI 服务。

## V3.1 执行口径

- 原始40日收益差只作观察，主信号使用5年滚动Beta调整后的差值。
- 页面同时显示2年、3年、5年Beta；窗口结论分歧时不执行交易动作。
- 调整后差值在5%到7%之间只暂停新增，不立即卖出。
- 战术减仓还必须满足红利自身40日上涨至少5%、站上MA250并连续2个收盘确认。
- 满足减仓条件时只处理战术仓约5%，核心仓另有更高门槛。
- 盘中数据不计入连续收盘确认，盘中不执行减仓。

## 数据与隐私

- 页面只监听本机 127.0.0.1，同一局域网其他电脑默认无法访问。
- 历史报告和缓存写在程序目录下的 reports、cache、latest.json。
- 建议把程序放在桌面、文档或其他有写入权限的目录，不要放在 Program Files。
